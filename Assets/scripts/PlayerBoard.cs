using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerBoard : MonoBehaviour

//create an option to make/choose different players/profiles
//each player has their own highscore(s)

//create a leaderboard with the highscores and players

//gameManager would reference chosen player from here and save/load their high score (playerPrefs?)


{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
